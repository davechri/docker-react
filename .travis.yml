language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t davechri/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true davechri/docker-react npm run test -- --coverage
